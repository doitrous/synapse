# LANE-BRIEF — MUST Year 1

## Identity

- University identity exactly as the academic package/spec records it: `must` | `MUST` | `MUST University`.
- Source root: `/Users/doitrous/Desktop/MUST`; Year overlay: `MUST_Y1`; terms: `Semester 101`, `Semester 102`.
- Scope is S0 reconciliation and S1 readiness only. No content record, canonical content-module ID, academic catalogue change, import, or publication is authorised here.

## Roots and evidence boundary

- This lane writes only under `docs/MUST-Source-Imports/` and, for the deterministic read-only counter, `scripts/must/`.
- Evidence remains read-only in the dirty root checkout: `.codex/library_audit/must/`, `.codex/library_audit/must_recovery/`, and `docs/import-ready/academic/generated-2026/academic-intake-package.json`; the Desktop source root is also read-only.
- The package and audit inputs are untracked WIP, so their raw contents are deliberately not copied into this branch. Their paths, package SHA-256 and exact counter output are recorded in the readiness file.

## Operating rules

- Priority: actual MUST papers and printed keys, then MUST department books/banks/atlases, then notes, then textbooks only as cited fallback.
- Search before minting. No canonical content-module IDs are inferred from the academic package.
- `TRIAGE APPROVED` is a reserved, literal checkpoint. It has **not** been issued: the proposed S1 lane must first read every selected printed question, recover keys, collapse tested concepts, and search live/pending/new before content exists.
- One future lane per `(module, department)` with a `CLAIMS.md` row before writing. Student-facing voice, if later authorised, must state medicine directly; provenance stays in metadata.

## Hazards

- Snapshot counts are not interchangeable: inventory, package, source manifests, and the current Desktop tree differ by scope/time.
- Five Year 1 failed manifest rows include two covered equivalents and three unresolved rows; no source fetch is authorised.
- The source audit and academic package are unreviewed root WIP. Their non-durable location blocks a portable S1 hand-off until Omar accepts/lands an evidence snapshot.

## Report format

Lane · produced (files/counts) · validation output · traceable-to-question share · blockers · next. Keep to 20 lines; quote gate/counter output rather than estimates.
