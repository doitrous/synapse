# iOS port — HANDOFF

Masterbrain handoff for the native iOS student app (**nishany**). Admin stays web; this is the student portal only.

## Production release track (approved plan: `~/.claude/plans/rippling-tumbling-bonbon.md`)
Full-parity + social production App Store release. Phase 0 (correctness blockers) status:
- **0.1 sync-key rebrand** `synapse.*`→`nishany.*` — DONE (staleness sync restored).
- **0.2 402 / paywall** — DONE (`APIError.paymentRequired`, `PaywallView`, `SyncEngine.subscriptionRequired`, `fullScreenCover` over paid tabs).
- **0.4 nishany build identity** — DONE (main already carried `com.nishany.app` / "Nishany"; my redundant rebrand reconciled during the rebase).
- **0.5a video→external-open**, **0.5b QBank offline cold-launch cache (G2)** — DONE (+ `LocalStore` v3-userdoc migration, `QBankOfflineTests`).
- **0.3 production repoint** — API half DONE + verified live. The old `synapse.doitrous.com` is retired (503); the live API is **nishany.com**, set in `Base.xcconfig`. The existing Supabase project (`ofifilywalzynftcctrn`) already pairs with production — the dev login (q@hotmail.com/000000) signs in and loads the Today screen live against nishany.com. Remaining 0.3: confirm this is the intended *production* Supabase (vs a separate prod project) with Omar, and get a non-test prod login for full-tier verification.
- **Rebased onto `origin/main` @ cd6319a19** (2026-09-14): 28 iOS commits replayed, my 0.4 taken as `--ours`/reconciled, force-pushed-with-lease. Next: Phase 1 (complete existing surfaces) → 2 (new study screens) → 3 (social/native voice) → 4 (IA+launch). Note main already shipped a Today-tab redesign that supersedes part of the Phase 4 IA plan.

### Live verification against production (2026-09-14)
Signed in on the sim against **nishany.com** with q@hotmail.com/000000. Confirmed live: the nishany rebrand (sign-in wordmark), the Today screen, and the Notebook rich editor round-trip (bold survives save/reload). The **402 paywall (Phase 0.2)** was also verified live while the account's trial was lapsed — a paid tab showed "Full access has ended" cleanly (not a "couldn't reach server" error) and "Browse free features" returned to Today with free tabs usable. Omar then granted the account a subscription, and after a refresh **all paid surfaces load with real production content** (Question Bank shows 2,895 questions; Library 139/161 articles). So both entitlement states are now verified: expired → paywall, subscribed → content. Still open in 0.3: confirm `ofifilywalzynftcctrn` is the intended *production* Supabase (vs a separate prod project) with Omar.

### Four-task polish session (2026-09-14) — all committed (`e36f79b26`) + pushed + verified live
Omar mandate: "update the nishany logo, Resources filters and dual-axis grouping, update the library look, move Study Rooms to a main tab."
1. **Logo** (`3581923fd`) — the bundled `logo`/`logo-dark` imagesets were the **stale blue Connect-era horseshoe**. Swapped for the crimson Noon (nun) mark (`public/brand/nishany-mark*.png`), and `Design/Wordmark.swift` now composes the lockup live in SwiftUI (mark + rounded "nishany" / "BY CONNECT") instead of a stale baked PNG. **Verified live:** header shows the crimson mark, old blue mark gone.
2. **Study Rooms → main tab** (`99442c5a4`) — tab bar is now **Today · Questions · Study Rooms · Library · More**. Flashcards demoted to a link in `MoreView` (Dashboard never routed to it, so nothing breaks); `FlashcardsView` dropped its own `NavigationStack` to push cleanly under More; "Study together" renamed "Study Rooms". **Verified live:** tapping Study Rooms opens `StudyTogetherView` (Join a test / Build a shared test / Your rooms).
3. **Library look** (`891f8b54d`) — the "How do you want to study?" chooser cards get a colored icon tile per view-kind (systems blue, discipline pink, skills green, knowledge amber, curriculum), matching-tinted count, trailing chevron. `Features/Library/LibraryView.swift` `palette(_:)`. **Verified live.**
4. **Subject catalogue + dual-axis Resources** (`e36f79b26`) — ported the web `CURRICULUM_SUBJECTS` map to `Core/Library/SubjectCatalog.swift` (20 subjects, English only; off-catalogue ids fall back to their own code like web `getSubject`). Resources gains a **By subject** grouping axis (segmented By chapter / By subject / By type) and the Question Bank names subjects through the same catalogue. `LibraryResource` carries `modules: [String]` for a future module axis. **Verified live:** "By subject" groups under "Cardiovascular" not "CVS". `ResourceProjectionTests.groupsBySubject` guards it.
   - **Still pending on Resources (audit items 7-remainder + 8):** subject/university/year *filters* (this was axes only), and the web's two-level System/Module grouping — the `modules` field is now plumbed for it.

### Phase 1 progress
- **Resources catalogue polish — type chips + location + subject axis (audit items 7 + 9 + part of 8).** Type filter chips (Book/Guideline/Deck/Article/Video) with live counts (shown only when >1 kind present) + the author's recorded location shown inline on a reference-only (no-file) row, mirroring the web. `Features/Library/ResourcesView.swift`. Dual-axis grouping (chapter/subject/type) via `ResourceModel.Grouping` + `SubjectCatalog`. **Verified live** on the KAU cohort: chips read "All 47 / Book 40 / Article 7", tapping "Article" filters to the OpenStax article entries, no-file rows show their location, and "By subject" names folders ("Cardiovascular"). **Still pending:** subject/university/year *filters* and web's two-level System/Module grouping (the `modules` field is now on `LibraryResource`).
- **Notebook rich-text editor — DONE + verified live.** Native inline bold/italic/underline/strike over the lossless Lexical model. `Features/More/RichTextEditor.swift` (a `UITextView` bridge with a B/I/U/S input-accessory toolbar — iOS-18 target, so not the iOS-26 attributed `TextEditor`), `Core/Notebook/RichText.swift` (`NotebookRichText`: `NSAttributedString` ↔ Lexical `editorJson`, marks → text-node `format` bitfield 1/2/4/8), `NotebookDoc.applyingRichEdit`, `Theme.uiFont`. Used only for `isSimple` notes; notes with headings/lists/tables keep the plain editor + notice so unmodeled structure is never flattened. `NotebookEditorTests.richMarksSurviveTheAttributedRoundTrip` guards the bitfield round-trip. Verified on the sim: created a note, bolded a run, saved to nishany.com, reopened — the bold survived the save/reload round-trip. **Later (Notebook polish):** in-place editing that preserves unmodeled blocks (edit text around a table/list without flattening); block types (headings, lists) in the native editor.

### Phase 3 (social) progress

- **Study Rooms — native hall (the party system) — DONE + verified live.** Replaced the iOS "Study Together" shared-quiz screen (kept in the tree for a later in-room activity) with the real live-hall **party** system (`server/src/parties.js` + `roomsRealtime.js`). Plan/spec: `docs/superpowers/plans/2026-09-18-ios-study-rooms-voice.md`, `docs/superpowers/specs/2026-09-14-ios-study-rooms-voice-design.md`.
  - New `ios/Synapse/Core/Rooms/`: `RoomMessages.swift` (Codable socket frames), `RoomChannelState.swift` (pure `reduceChannel` port of `roomChannel.ts` + `roomSocketURL`/`backoffDelay`), `RoomChannel.swift` (`URLSessionWebSocketTask` actor; **auth = bearer in the WebSocket subprotocol** `["nishany.bearer", <supabaseAccessToken>]`; requestId reply correlation, 20s ping, 1s->30s backoff, 4401/4403 permanent close), `Party.swift` (REST models), `RoomModel.swift` (`@MainActor @Observable`, merges socket + REST, 30s heartbeat). Party REST added to `SynapseAPI` (`createParty/joinParty/myParties/openParties/party/leaveParty/roomHeartbeat/roomMembers/blockUser`). `AuthModel.tokenProvider` exposes the token for the socket.
  - New `ios/Synapse/Features/Rooms/`: `RoomsLobbyView` (create / join-by-code / your-rooms / open-in-year), `RoomHallView` (named apart from the legacy shared-quiz `RoomView`), `RoomRosterView` (speaking ring, host tile, whisper/mute/block context menu), `RoomChatView` (public + whisper, own-line right-aligned), `RoomDock`. Wired into `SignedInView` (Study Rooms tab root + app-wide dock).
  - **Verified live on the sim against nishany.com** (q@hotmail.com, KAU-Y1 cohort): lobby lists a real party; opening it **connects the socket and shows live presence** (3 real members: doitrous/omar/Q-you, self-marked, host tile crimson); **chat round-trips** (own line rendered from the server echo); **minimize -> dock** ("Study room / 3 people here") persists the live room **across tab switches**; reopen keeps the socket + chat history. **Voice (Phase B) not yet built.**
  - Bug fixed during verify: the dock was a `.safeAreaInset(edge:.bottom)` on the `TabView`, which **consumed the tab bar** (stranded the student on one tab while minimized) -> changed to an `.overlay(alignment:.bottom)` lifted 49pt above the bar (mini-player pattern).
  - **Known minor robustness item (not blocking):** the lobby's initial `.task` load and `api.party(id)` "Open" can transiently fail silently right after launch (token not yet ready) — recovered on pull-to-refresh / retry in testing. A small retry/backoff on these reads is a good follow-up.
- **Study Rooms — voice (Phase B, mediasoup) — NOT STARTED.** Vendored `mediasoup-client-swift` xcframeworks + mic/background-audio Info.plist + `RoomAudio`/`VoiceActivity` + voice UI. Per the spec, **real two-way audio can't be exercised on the simulator** (needs two physical devices) — final voice sign-off is Omar's on hardware; the handshake will be traced against the live SFU.

## Where the work lives
- Branch: **`claude/cortex-ios-port-79b661`** (pushed to origin). **Push is over SSH**: the remote is `git@github.com:omary98/synapse.git` and the account's key is `~/.ssh/id_hetzner` — a non-default name that is *not* loaded in the agent, so a bare `git push` fails with `Permission denied (publickey)`. Push with `GIT_SSH_COMMAND='ssh -i ~/.ssh/id_hetzner -o IdentitiesOnly=yes' git push origin <branch>`, or have Omar run `ssh-add ~/.ssh/id_hetzner` once. iOS app in `ios/`, web reference in `src/`, Express API in `server/`.
- Build in a **dedicated worktree**, not the main checkout (content lanes reset it). This branch touches only `ios/` and `docs/ios/` — content lanes touch `docs/`, `scripts/kasr/`, batches — so a rebase onto `origin/main` is clean.

## Baseline / how to build + test
- **iOS: 729 tests pass** (was 694). Server: 385 pass.
- Build+test: `xcodebuild test -project ios/Synapse.xcodeproj -scheme Synapse -destination 'platform=iOS Simulator,id=643D5D39-4A1F-469B-AEDA-5AFDDE97BCAE'` (sim **Nishany-QA**, OS 26.5; there is no "iPhone 17" sim on this machine). **Never pass `CODE_SIGNING_ALLOWED=NO`** (breaks Keychain → silent 401). ~1–4 min.
- `Secrets.xcconfig`: copy from the main checkout (`cp <main>/ios/Config/Secrets.xcconfig ios/Config/`). node_modules + `server/node_modules`: symlink to the main checkout.
- Sim login: **q@hotmail.com / 000000**.

## Done this session
1. **Navigation IA** (`92798bc5`) — daily-driver tabs: **Today · Questions · Flashcards · Library · More**. New `FlashcardsView` placeholder; Resources moved to a folder button in the Library toolbar. (Omar's decision.)
2. **Flashcards data + store layer — COMPLETE** (`6f919270`, `7fbe5e53`, `71c2af6e`, `9aa7422f`, `bb211ba1`, `9d81fc39`), all under `ios/Synapse/Core/Flashcards/`, each pinned to the web's own `*.test.ts`:
   - `CardSchedule` + `SM2Scheduler` + `LocalDay`; `Scheduler` protocol + `FSRSScheduler` (matches published FSRS-5 reference values); `FlashcardModel` (notes/cards/collection, JSON round-trips byte-for-byte); `ClozeParser` + `CardGen`; `FlashcardActions` + `FlashcardStatus` + `FlashcardQueue`; `FlashcardStore` (@MainActor @Observable, user-state sync).
3. **Audit** (`e2712d52`) — `docs/ios/audit/`: PARITY-MATRIX + specs for flashcards, offline-questions, resources-editor, whiteboard.

## Decisions on record (Omar)
- Nav IA = daily-driver tabs (above).
- **Resources** = full web parity (catalogue + "My uploads" file locker + flawless PDF annotation) **PLUS a new in-app rich-text editor** (beyond web).
- Download policy = Wi-Fi-only default + manual override (my call, revisable).
- Build a **shared "My Documents" chunked-upload client** — unblocks Resources uploads AND Whiteboard images.

## Gotchas (learned the hard way)
- **Simulator input injection needs a full ⌘Q quit+reopen of the Claude app** to take effect (Accessibility TCC). Screenshots work without it; taps/typing silently no-op until the restart. After restart, login flow works (tap 200,306 email / 200,371 password / 200,447 sign-in in the 402×874 pt space; verify focus by screenshot — caret blinks).
- **Subagents stall on a 600s no-output watchdog** — 3/3 code-port agents AND 1 audit agent died this way. Build code ports **yourself** in small committed slices. (Wave-1 audits happened to succeed, but don't rely on it.)
- **Type-name collisions**: web `Note`→iOS **`FlashcardNote`** (Notebook has `Note`); web `ShapeKind`→**`OccluderShapeKind`** (Reader has `ShapeKind`). Grep new type names against the module first.
- **Synced payload fidelity**: `CardMeta`/`ReviewEvent` use a custom `encode(to:)` to write explicit `null` (not omitted) like the web; SM-2 `CardSchedule` omits `stability`/`difficulty`. Reuse `ISO8601DateFormatter.synapse` (+ `.read`) so `due` round-trips with JS `toISOString()`.
- **Cloze** uses `NSString`/`NSRegularExpression` (UTF-16 offsets) to match JS string semantics.
- `Synapse/` is a synchronized file-system group — new files under it are auto-added to the app target. A **new extension target (widgets) is NOT** — that's a real `.pbxproj` change.

## Flashcards — feature-complete for this scope (verified on the sim)
Study-loop UI, Basic + **Cloze** authoring and rendering, the **FSRS opt-in deck toggle**, and **provided (catalogue) decks** are all shipped and verified live (create deck → add Basic/Cloze card → study with correct SM-2 *and* FSRS intervals → grade/advance/session-complete). `FlashcardStore` reads through a merged view (own collection + provided decks). Files: `Core/Flashcards/*`, `Features/Flashcards/*`, `Core/Flashcards/DeckProjection.swift`, `ContentKind.deck`.

**Remaining Flashcards polish (later):** Browse/stats view, GRDB offline cache (flashcards.md §4.2), card audio + image-occlusion authoring, rich-text rendering (currently HTML is stripped to plain text in `FlashcardStudyView.plain/unhtml`), quick-add from Question Bank/Reader. No catalogue decks exist for the KAU Y1 test cohort, so the provided-deck path is unit-tested and will show a "Shared" deck once one is published.

## Shipped since (wave-1)
- **Question Bank pull-to-refresh** (`d89bffb1`) — the audit's missing-refresh gap.
- **Whiteboard sync fix** (`21a624cb`) — iOS now reads/writes the live `nishany.whiteboard.boards.v1` multi-board document and **preserves every element type** (ink/images/files/ready-items) it doesn't render, so a phone save never strips a web board. Legacy single-board drawings migrate on first load. Verified live: a legacy board migrated and the edit wrote `boards.v1` (`migratedFromSingleBoard:true`) — confirmed in the outbox.
- **Whiteboard pen / eraser / undo (tool coverage)** (`4f6a28dcf`) — the headline touch-whiteboard tools. Pen (5 web ink colours `ink/primary/danger/success/warning`, 3 widths `2/4/8`), eraser (drag rubs out a whole stroke via a fattened hit-path, `BoardGeometry.strokeHit`), ink above/below-notes toggle, and a 50-entry session undo/redo stack every edit records into. Strokes use the web's flat `InkStroke.points` array + quadratic-through-midpoints smoothing, so a line round-trips identically to a browser. Gesture routing by tool: one-finger drag draws/erases, select pans, pinch-zoom stays live. `Core/Whiteboard/Whiteboard.swift` (`WhiteboardTool`, `BoardInk`, `strokeHit`), `Features/More/WhiteboardView.swift`. **Verified live**: drew smooth strokes, colour/width pickers, ink rendered *behind* notes, eraser removed a stroke, undo/redo stepped correctly. `WhiteboardTests` +4 (`Ink` suite). **Ceiling** (ponytail): SwiftUI `DragGesture` capture, not the Reader's coalesced-touch/pressure `InkCaptureView` bridge — no 240 Hz Pencil smoothing yet; and a stroke *started within ~20 pt of the left screen edge* can trigger the NavigationStack back-swipe (inherent UIKit edge-pan; rare on a centred 8000-wide board). **Whiteboard still missing** (audit build-order): z-order, link bend/straighten/standalone-delete, ready-items (needs a 51-icon asset design call), minimap, sharing. **Now present** (~16 of 24 audit rows, was 7): select/pan, zoom, fit, bounds, notes CRUD, note colour, links (two-tap), pen+ink colours/widths, eraser, ink above/below, undo/redo, frames CRUD, multi-board CRUD, note search, pictures/files.
- **Whiteboard note search** (`fc9f4bad3`) — a find bar off a toolbar button over the existing `BoardGeometry.matching`: n/total, up/down step-through (wraps), recentres the view on each match and selects it. Session-only. **Verified live**: "Synced" → 1/1, view recentred on and selected the matching note.
- **Whiteboard multi-board switcher** (`3346007ab`) — a boards sheet off a toolbar button (`square.stack`): every board with note count + active checkmark, tap to switch, + to create, swipe to rename (alert) / delete (last board kept). Switch resets undo+selection and adopts the new board's `BoardState`; the whole `WhiteboardCollection` writes through the synced key. `WhiteboardTests.Collection` (+4). **Verified live**: created a board (canvas cleared), switched back (5 notes returned), swipe-deleted it (focus moved to Default). The pure collection helpers (`Whiteboards.*`) already existed from the sync fix — this was UI only.
- **Medical Terminology** (`8819e84cc`) — rebuilt the glossary surface into the web's page. The old `GlossaryView` was a bare term/translation list that mis-decoded the doc (read a top-level array; the real `GlossaryDoc` is `{version, categories, terms}`), so on live data it showed "No glossary yet". Now `TerminologyView`: bilingual term + EN/AR definitions + example (tap to reveal), a category rail with counts, a "Terms you know" progress bar, and a per-term "Got it" toggle. Progress ports `terminologyProgress.ts` exactly — same shape, same key `nishany.terminology.progress.v1` — so a term marked known on the phone is known on the web. Two fixes surfaced: `loadProgress` treats a 404 (never-written user-state key) as an empty writable doc (a new student could otherwise never mark a term); and `StateOwnership` was missing `^nishany\.terminology\.` so the first write tripped the `isUserOwned` assertion (debug crash / 403 in release) — server already allows it. `TerminologyView.swift` (new), `MoreView` (rewired, renamed "Medical taxonomy"→"Medical Terminology"), `GlossaryView` deleted from `OtherViews`. `TerminologyTests` +3, `StateOwnershipTests` +1. **Verified live**: 297 bilingual terms, reveal shows AR+EN defs, marking a term persists across a cold relaunch (server round-trip), unmarking restores. **Skipped**: crossword + term-match minigames (Phase 3), make-a-flashcard-deck shortcut. Related: bookmarks had the identical 404 bug, fixed in `14b9d8fad`.
- **Question Bank written formats** (pending) — the study loop only ran single-best MCQs. `QuestionProjection` dropped every other format because it required an option whose label matched `correctAnswer`, and a written question's `answers` are the fixed A-F placeholders, all blank -> zero options -> dropped. On the live ledger that silently hides **285 written questions** (`structured_written` 158, `multipart_written` 50, `short_answer` 42, `comparison_table` 35). Now written formats (those five, per the web `WRITTEN_FORMATS`) project from their `writtenParts` mark scheme instead of being dropped, and run as **self-graded**: the student answers on paper/in their head, taps **Reveal model answer** (each part shows its marks + the expected points, plus the question's model answer/objective), then grades themselves **I knew this / Needs review**. The grade routes through the *existing* option pipeline via a `selfCorrectLabel` sentinel graded against — so accuracy, the navigator, resume and the concept ledger all count it with **no special case** (written question's concepts = its own tags + every part's). `true_false` already ran as a genuine 2-option MCQ and is untouched; `matching`/`completion` (12 questions, Year-1 only, no runner) still fall through to the choice guard and drop — a missing question beats a set of blank buttons. `Question.swift` (+`WrittenPart`, `format`, `writtenParts`, sentinels, written projection branch), `QuestionBankModel.swift` (+`revealedWritten`, `revealWritten`/`selfMark`/`writtenGrade`; resume keeps only choices it could restore), `QuestionBankView.swift` (+`writtenBody`/`writtenPart`/`selfMarkRow`). `QuestionProjectionTests` +6, new `WrittenQuestionTests` +5. **MCQ path verified live (regression)**: choose -> Check -> reveal + wrong-answer disclosure + "1 correct" all intact. **Written runner NOT visually verified live**: every non-MCQ question in the ledger is Year 1 (KAU_Y1/HU_Y1/AU_Y1) and this test account (q@hotmail.com) is roster-locked to **KAU Year 2**, so none are in scope — the plan flagged this ("Blind vs KAU-Y1 - use a cohort with formats to verify"). Logic is covered by the 11 unit tests against the real question JSON shapes; a visual pass needs a Year-1-scoped account. **Deferred**: `mcq_multi`/`labeling`/`essay`/`image_based` (none exist in the live data), matching + completion runners, question media (`image_based`, no media in this cohort).
- **Minigames — Term Grid** (this session) — second native minigame, a crossword built from the glossary, a port of the web `TermGridPage`. In the **Minigames** hub above Term Match. A **category** picker (only categories with the crossword's floor of >=8 usable terms are offered) draws that category's glossary terms; `TermGrid.buildGrid` lays them into an interlocked, cropped, numbered grid, refusing below 8 interlocked words. Two words come **given** (pre-filled grey, deterministic from the seed). Same share-link promise as Term Match: `https://nishany.com/app/term-grid?category=X&seed=Y` rebuilds the identical grid on phone or web, which holds because `TermGrid` is a **bit-exact port of `src/data/crossword.ts`** — the greedy best-fit placement (smallest bounding box, then most crossings, then top/left/across), the 8-seed retry window striding by `0x9e3779b1`, and the seed->grid determinism all reproduce the web square-for-square (reusing the shared `SeededRandom`). Pinned by a `parity` test against the web's actual `buildGrid` output for a fixed 12-term set + seed (captured via `node --experimental-strip-types`, not guessed). **Input is a `UIKeyInput` off-screen first responder, NOT a bound `TextField`** — a first cut bound a hidden `TextField` and diffed its string, and fast typing/paste dropped characters (the injected keys raced the binding's reset; live test showed "SPHINCTER" landing as "SPHIN"); `UIKeyInput` delivers each `insertText`/`deleteBackward` directly, so nothing is lost. A cell belongs to up to two words; tapping a filled square flips across<->down, typing fills and advances along the active word (correct letter inks black, wrong inks red), Backspace clears/steps back, a per-clue **Reveal** fills a word (kept apart from the solve), completion pops a time + revealed-count + New-puzzle panel. `Core/Minigames/TermGrid.swift` (pure generator: `GridTerm`/`PlacedWord`/`Grid`, `buildGrid`/`givenTerms`/`normalizeAnswer`), `Features/Minigames/TermGridView.swift` (`TermGridModel` reuses the glossary load, board + `KeyCatcher`), `MinigamesView.swift` (+row). `TermGridTests` +6 (grid parity, determinism, seed-varies, refuse-below-min, normalisation, well-formed). **Verified live** (q@hotmail.com, KAU Y2 — glossary in scope): category "Directional & anatomy" builds a real interlocked grid, given words grey, typing TENDON into 4-across fills+advances all six with no dropped keys, wrong letters red, clue bar tracks the selection, a new seed yields a different puzzle. **Deferred**: cross-device synced progress (`nishany.termgrid.progress.v1` — like Term Match, each round is local; syncing needs a server `USER_OWNED_PATTERN` + iOS `StateOwnership` entry).
- **Minigames — Term Match** (this session) — first native minigame, a port of the web `TermMatchPage`. New **More → Minigames** hub (`gamecontroller` link after Medical Terminology) → **Term Match**: a pairs board over the shared medical glossary (`SyncEngine.glossaryKey`, the same doc the Terminology page reads), matching each term to its **Arabic** translation or its **definition** (segmented picker). 8 pairs by default, refuses below 6 usable terms. The whole board is built from one seed — which terms are chosen *and* how the answer column is shuffled — so a **share link** (`https://nishany.com/app/term-match?seed=X&mode=Y`) rebuilds the identical board on the phone or the web, tile-for-tile. That only holds if the phone's RNG is bit-identical to the web's: `SeededRandom` is a hand-verified port of `src/data/seededRandom.ts` (splitmix mix + xorshift32 in **UInt32** arithmetic, matching JS `Math.imul`/`>>>`), pinned by an `rngParity` test against the web's actual first three draws (captured via node, not guessed). Interaction: tap two tiles, correct → green + disabled, wrong → 650ms flash then clears (the web's beat), finish panel shows time + wrong attempts + Play again; wrong-answer count feeds nothing else, it's a local round. Files: `Core/Minigames/TermMatch.swift` (pure generator: `SeededRandom`, `MatchMode`, `MatchTile`, `MatchBoard`, `TermMatch.buildBoard`/`isPair`), `Features/Minigames/TermMatchView.swift` (`TermMatchModel` loads glossary, board keyed on `mode-seed` so a new game remounts clean, `ShareLink`), `Features/Minigames/MinigamesView.swift` (hub), `MoreView.swift` (+link). Reuses the top-level `MedicalTerm` from `TerminologyView`. `TermMatchTests` +7 (determinism, seed-varies, RNG parity, size, skip-unmatchable, refuse-below-min, pairing). **Verified live** (q@hotmail.com, KAU Y2 — glossary IS in scope, unlike QBank written formats): board renders from the live glossary, Arabic tiles right-to-left; correct match (Antiviral ↔ مضاد للفيروسات) turns green + disables; a mismatch flashes and clears; switching to Definition remounts a fresh board with LTR English definitions. **Deferred**: the other 5 web games (Term Grid, Spotter, Clinical Sequence, Mechanism Chain, Red Flag Sort — over authored packs) light up in the same hub as they arrive.
- **Account field audit + prefs-doc parity fix** (`0f0990190`) — audited the web Account page (`src/pages/student/Account.tsx`, 4 tabs) against the iOS `AccountView`. **Root bug found & fixed:** both platforms write the *same* shared doc `nishany.account.prefs.v1` last-write-wins, but their shapes disagreed — web writes `{reviewReminders, calendarReminders, dailyGoalQuestions, reminderTime}`, iOS wrote `{timezone, reviewReminders, calendarReminders}`. So iOS's save (e.g. `syncTimezone` on launch, or any reminder toggle) silently **erased** the web's `dailyGoalQuestions`/`reminderTime`, and a web save erased iOS's `timezone`. Worse, iOS's `timezone` field was dead: the server reads a student's timezone from their **profile row** (`qotdReminders.js` `SELECT s.timezone`), set by `PUT /api/me/profile`, never from this doc — so iOS-only students always got reminders on Cairo time. Fix: `AccountPrefs` now mirrors the web's exact 4-field shape (default `dailyGoalQuestions:30`, `reminderTime:"19:00"`); `timezone` removed from it and `syncTimezone` now calls the new `SynapseAPI.putProfile(timezone:)` (guarded by a per-device UserDefaults "last pushed" so it PUTs only when the zone moves). Surfaced the **Daily question goal** as a +/- stepper (plain buttons, not `Stepper` — same stock-control-swallows-taps issue `switchRow` documents) in a new Study preferences section, and wired `DashboardView`'s Today ring to it (was a hardcoded 40; now the student's goal, default 30, `TargetRing` already guards goal 0). Reloads on Account-sheet dismiss so the ring updates without a relaunch. **Deferred / reported (not built):** `reminderTime` (no consumer on iOS or server — carried losslessly in the model, no UI); social/Phase-3 profile fields (avatar, username + availability, profile icon, status message, "let classmates find me" discoverable toggle → `GET/POST /account/discoverable`); security (in-app password change `PUT /auth/password`, sign-out-all-devices, passkeys, MFA — plan says MFA may defer); data export (`/account/export`); email-suppression list; enrolment-change request (iOS cohort is roster-locked / self-declare only). `AccountPrefs.swift`, `SynapseAPI.swift` (+putProfile), `SignedInView.swift` (goalRow + Study preferences section, timezone row now `TimeZone.current`), `DashboardView.swift` (goal from prefs + reload-on-dismiss). `AccountPrefsTests` reworked (+lossless 4-field round-trip guard, +negative-goal clamp). **Verified live** (q@hotmail.com, KAU Y2): goal steps 30->40->30 with Sync "Up to date", reopening Account re-reads the server value (round-trip), Today target reads the goal ("0 of 30"), time zone shows Africa/Cairo; clean cold-launch, no crash. Test account left at default 30.
- **Performance summary parity** (`31888050b`) — the web "My Analytics" leads with figures the iOS model already computed but never showed: distinct questions seen (`covered`, not tries) and time per question. The headline is now a 2x2 stat grid adding both. Also fixed a latent bug: `summary.medianSeconds` was `totalSeconds / attempts` — a *mean*, over untimed answers too, mislabelled a median; now `summarise` computes a real median ("Typical time") and a mean ("avg") over **timed answers only**, matching the web's usual/average. `PerformanceModel` (`medianSeconds`/`averageSeconds` stored), `PerformanceView` (grid + `timeLabel`). `PerformanceTests` +2. **Verified live**: Questions seen 26 of 26 tries; Typical time 1m 27s / avg 1m 40s (median and mean correctly differ). **Deferred** (need data the attempt store lacks): study-habits panel (answer changes + highlight focus), time-studying-by-category panel, and the whole **Class Leaders** leaderboard tab (needs cohort data + a server contract).
- **Resources two-level grouping** (`da0cd1636`) — a faithful port of `src/data/resourceGrouping.ts`. The old iOS grouping was one-level and inverted (its "system" bucketed by *chapter*, plus a subject/kind axis the web lacks, with no chapter subfolders). Now the web's two axes over the same stored `nishany.resources.groupBy` (default **Module**): the primary folder is a **subject** (System) or a **module** (Module), each split into **chapter subfolders** (the chapterless ones sit flush under the folder). Nothing is dropped for an off-catalogue subject/module — catalogue subjects keep catalogue order (`SubjectCatalog` gained an `order`), the rest follow, the nameless bucket last; modules sort naturally so "CVS 2" precedes "CVS 10"; folder headers carry a count. `ResourceModel.group`+`Subfolder`, `ResourcesView` (subfolder rendering + filter-within-subfolder). **University/year filter dropdowns deliberately skipped**: the device only holds in-scope resources (`LedgerItem.inScope(audience)`), so both would be single-option — the web filters those client-side only because it fetches the whole catalogue. Grouping test suite reworked (11-case port of `resourceGrouping.test.ts`). **Verified live**: Module → "No module (47)" folder of chapter subfolders; System → "Cardiovascular (8)" etc. named from the catalogue; type chips filter within the subfolders.
- **Whiteboard pictures / files** (`2f1e62ef6`) — the biggest remaining tool. The toolbar + gains **Picture** (PhotosPicker) and **File** (document importer). A pick uploads to the student's document store via the new `SynapseAPI.uploadMyDocument` (one-call create+chunk+complete, returns the id) and is referenced on the board by `documentId` — the web wire format, not inlined, so a board stays small and the same bytes back the Library locker. Pictures render **behind** notes (`BoardImageView`, downloads by id, `BoardMediaCache` NSCache, legacy inline `data:` src still renders), drag to move, corner-resize with the aspect ratio locked (min 60x45); files render as a card (`BoardFileCard`: icon/name/size) that opens in a QuickLook sheet (`BoardFilePreviewView` reusing the now-non-private `QuickLookPreview`). Both delete from the selection toolbar; all record undo. `BoardGeometry.imageWidth/fileSize/minImageSize`. **Verified live**: + → Picture → picked a photo → uploaded → rendered → moved → aspect-locked resize → deleted; the File importer opens (sim Files empty, so the card/preview lean on the already-verified My-uploads QuickLook path). **Ceiling** (ponytail): drag/resize via `DragGesture`, no rotate; file card/preview unexercised on a sim with no files.
- **Whiteboard sections/frames** (`c3b944eb8`) — the frame model was decoded+preserved but never drawn; now rendered as dashed grouping rectangles with a title bar. Add via a Note/Section menu on the toolbar +, drag to move (carries the notes whose centre is inside, `BoardGeometry.notesInFrame`), a corner handle to resize (min 200x140), a rename sheet, delete from the selection context row; all record undo. `WhiteboardTests.Frames` guards notesInFrame. **Verified live**: added a section, renamed it, it rendered behind the notes with a solid selected border, undo reverted the add/rename/move chain.
- **Resources "My uploads"** (`2246f08b`) — the personal document locker over the chunked `/api/my-documents` REST contract (`SynapseAPI` list/create/upload-chunk/complete/rename/delete/download; `MyDocumentStore`; `MyUploadsView` reached from a Resources toolbar button). List + upload (PhotosPicker / fileImporter) + rename + delete + usage. **Verified live** (this session): uploaded a photo → appeared as "Photo · 4.1 MB", usage bar updated.
- **Open an uploaded document** (`958e7cf9`) — tapping a My-uploads row downloads the file (bearer-authed) and previews it in `QLPreviewController` (PDF paging / image zoom / share/print), completing the upload→open loop. `MyDocument.previewExtension` gives the extensionless download the right extension so QuickLook renders it instead of raw bytes; `DocumentPreviewLoader` parks it under a safe, id-keyed, extension-carrying temp URL (cached for the session). Verified live: uploaded a photo, opened it, image rendered correctly. **Still to do for Resources:** the `ResourceFileStore.swift:61` video→`.pdf` bug (a `.video` resource still downloads as a PDF).
- **Notebook — lossless rich-text note model** (`cca57872`) — the "rich-text editor" work, **Phase 1**. The web notebook is a full **Lexical** editor: a note's real content is `editorJson` (+ `plainText`/`legacyMarkdownSource`/`revision`), with pasted media in `imageDocumentId` and freehand ink in `drawing`. The iOS `Note` struct modelled **none** of those — so, because the notebook is one array rewritten whole, editing any note on the phone **stripped rich-text + ink from every note on the web** (the exact loss `NotebookView`'s own doc-comment warned about), and could even fail to decode a modern note since the web's save payload omits `body`. Now: `Note` carries every web field (`editorJson`/`drawing` as opaque `JSONValue` — reuses `Core/Model/JSONValue.swift`), `body` is optional, and the web's text helpers are ported faithfully to `Core/Notebook/NotebookDoc.swift`. The editor reads via `notePlainText` and saves via `applyingEdit` (regenerates `editorJson`/`plainText`, bumps `revision`, preserves ink/refs/images), and shows a banner before it simplifies a web-formatted note rather than flattening it silently. A decode→encode round-trip test guards the invariant. Verified live: create/edit/save a note; the Notebook loads and the preview reads the modern fields.

## Fixed
- **`SynapseAPI` concurrent-request crash** (`923807fa`) — `lastDiagnostic`/`lastTokenError` were unlocked `nonisolated(unsafe)` statics assigned from concurrent `send()` fan-outs, double-freeing the Optional's buffer (`_swift_release_dealloc` / EXC_BAD_ACCESS). Now serialized behind an `NSLock`; `APIConcurrencyTests` is a TSan regression guard. This was also what made the test-host crash at bootstrap and the app launch blank intermittently.

## Next, in order
1. **Widgets + focus timer** — BLOCKED on a new Xcode extension target (objectVersion-77 pbxproj; risky to add by hand). Needs a one-time Xcode "New Target → Widget Extension + App Group" from Omar; then build the shared snapshot + widgets + ActivityKit focus timer on top.
2. **Question Bank images** — the media model + `MediaFileStore` (auth'd `/api/media/:id`) + rendering. NOTE: the KAU-Y1 test cohort has **zero** questions with images (checked the ledger), so this is a blind/unit-tested build until content has one.
3. **Notebook rich-text editor — DONE** (bold/italic/underline/strike; see the Phase 1 progress section above). Remaining notebook polish: block types (heading h1–h4, quote, bullet/numbered/check lists) in the native editor, and in-place editing that preserves unmodeled blocks (tables/images/HR) rather than the current `isSimple` plain-editor fallback. Web reference: `src/components/notebook/NoteEditor.tsx` (Lexical).
4. **Wave-2 audits+build**: University, Essay Questions, Maristanas, Minigames. (Calendar + Medical Taxonomy already exist on iOS.)
5. **Flashcards polish**: Browse/stats, GRDB offline cache, audio/occlusion authoring, rich-text rendering. Plus QBank offline cold-launch fallback (bug G2) + question-media rendering.
   - Widgets detail when unblocked: App Group + a shared Codable snapshot the app writes on sync; widgets for Today/Flashcards-due (`FlashcardStore.entries` is `nonisolated` for this) / Everyday Question; Focus timer as an ActivityKit Live Activity (`Features/Reader/StudyTimer.swift` to generalize).

## Rules
Small commits, explicit `git add ios/`. Rebase + re-fetch `origin/main` before every push. Never touch `package.json` or `scripts/kasr/`. Verify UI on the simulator (q@hotmail.com/000000).
