| cluster | authored | held | remaining | total |
|---|---:|---:|---:|---:|
| anatomy-owed | 18 | 0 | 0 | 18 |
| anatomy-wagih | 25 | 5 | 0 | 30 |
| anatomy-wagih-embryology | 32 | 0 | 0 | 32 |
| enz | 45 | 1 | 0 | 46 |

## Held
- anatomy-wagih-hold01 — p2 Q2 (elbow joint is considered a type of: hinge, keyed b) — near-identical duplicate of the already-landed anatomy-owed q11 (same fact, same concept CON-MSK-4D7492BC85C03D: elbow = hinge joint). Not re-authored.
- anatomy-wagih-hold04 — p4 Q4 (largest sesamoid bone: patella, keyed a) — near-identical duplicate of the already-landed anatomy-owed q05 (same fact, same concept CON-MSK-00B4A0D32A6420). Not re-authored.
- anatomy-wagih-hold06 — p6 Q3 (triceps brachii in elbow extension acts as: keyed e, 'both a & c' — prime mover AND antagonist) — this Wagih table's own recovered key directly conflicts with the already-landed anatomy-owed q16 record on the identical fact (same concept CON-MSK-F598AF39FBE297, Nebras-bank key 'd', prime mover only). Authoring a second record with the opposite correct answer on the same stem would misinform rather than add coverage; held per LANE-BRIEF's convention-clash guidance rather than guessed or silently dropped.
- anatomy-wagih-hold07 — p6 Q4 (flat sheet-like connective tissue attaching muscle to bone: aponeurosis, keyed b) — near-identical duplicate of the already-landed anatomy-owed q15 (same fact, same concept CON-MSK-4018ED42ADDDB4; that record was authored from the concept body since the key was unrecovered at the time — now recovered here and it matches). Not re-authored.
- anatomy-wagih-hold08 — p6 Q5 (the appendicular skeleton contains ___ bones: keyed d, 206) — stem/key mismatch: 206 is the whole-body adult bone total, not the appendicular skeleton's own count (126), which is not among the printed options at all. Not a convention clash (there is no coherent alternate reading that makes 206 the appendicular count) — held as an uncorrectable source error rather than authored with a misleading correct answer.
- enz-q53 — Re-read directly (pagetext.mjs show, p.12 stem + p.14 printed answer table -- a clean printed list, not a highlight/OCR mark, so the LETTER is unambiguous: '53. a'). The letter mechanism is certain; the content is not. Stem: enzyme A reaches half-max velocity at 0.05 mM substrate, enzyme B at 5 mM -- this establishes only that enzyme A has the lower Km (higher affinity); it gives no Vmax data at all, so no option's Vmax clause is derivable from the stem. Two of the four options (a: 'lower Ki and faster Vmax', b: 'lower Kim and slower Vmax' -- 'Ki'/'Kim' both read as a typo for 'Km') are the only ones directionally correct on Km; they differ solely in an equally unsupportable Vmax claim (c and d are eliminated outright, both getting the Km direction backwards). The print picked (a), but nothing in the stem corroborates 'faster' over 'slower' Vmax for enzyme A over (b) -- this is a genuine defect in the bank's own question (it asserts a comparison its own data cannot support), not a convention clash or an OCR ambiguity, so held rather than editorial-keyed. Exact ambiguity for the ledger: options A and B are the two the print's Km-direction reasoning supports; neither Vmax clause is derivable, so the choice between them is unresolvable from the source.

## Remaining
(none)
